# PWM-motror-control
control motor speed using PWM using arduino.
The speed is controlled manually using serial monitor.the speed is controllled in a way that typing in a format 'speed pwm value'.
This whole process can be interupted using the command "e_stop".

